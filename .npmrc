# npm ignores workspace members' own .npmrc files, so aza/.npmrc no longer applies once the root
# package.json declares workspaces. This restates it at the root, where installs actually run —
# including the install EAS performs for cloud builds.
legacy-peer-deps=true
